import React from "react";
import PropTypes from "prop-types";
import { makeStyles } from "@material-ui/core/styles";
import Tabs from "@material-ui/core/Tabs";
import Tab from "@material-ui/core/Tab";
import Typography from "@material-ui/core/Typography";
import Box from "@material-ui/core/Box";
import FadeInSection from "./FadeInSection";

const isHorizontal = window.innerWidth < 600;

function TabPanel(props) {
  const { children, value, index, ...other } = props;

  if (isHorizontal) {
    return (
      <div
        role="tabpanel"
        hidden={value !== index}
        id={`full-width-tabpanel-${index}`}
        aria-labelledby={`full-width-tab-${index}`}
        {...other}
      >
        {value === index && (
          <Box p={3}>
            <Typography>{children}</Typography>
          </Box>
        )}
      </div>
    );
  } else {
    return (
      <div
        role="tabpanel"
        hidden={value !== index}
        id={`vertical-tabpanel`}
        {...other}
      >
        {value === index && (
          <Box p={3}>
            <Typography>{children}</Typography>
          </Box>
        )}
      </div>
    );
  }
}

TabPanel.propTypes = {
  children: PropTypes.node,
  index: PropTypes.any.isRequired,
  value: PropTypes.any.isRequired
};

function a11yProps(index) {
  if (isHorizontal) {
    return {
      id: `full-width-tab-${index}`,
      "aria-controls": `full-width-tabpanel-${index}`
    };
  } else {
    return {
      id: `vertical-tab-${index}`
    };
  }
}

const useStyles = makeStyles(theme => ({
  root: {
    flexGrow: 1,
    backgroundColor: "theme.palette.background.paper",
    display: "flex",
    height: 300
  },
  tabs: {
    borderRight: `1px solid ${theme.palette.divider}`
  }
}));

const JobList = () => {
  const classes = useStyles();
  const [value, setValue] = React.useState(0);

  const experienceItems = {
    "Principal Financial Group": {
      jobTitle: "Software Development Engineer Intern @",
      duration: "JUN 2023 - PRESENT",
      desc: [
        "Collaborate with experienced engineers to conceive, design, and bring innovative products and services to market.",
        "Developing an application that sits on a user desktop that gathers investment data and generates a PDF that gets published on the public Principal website. Viewable globally by internal business partners.",
        "Typescript, Java, and JavaScript based application, using Spring framework, REST calls, CMPsUI on React, APIS, Java Batch, and Java store (STPS). Project database is DB2."
      ]
    },
    "Husky Coding Project": {
      jobTitle: "Co-Chair @",
      duration: "JUN 2023 - PRESENT",
      desc: [
        "Led a student tech-club’s initiative to provide team-based programming project experience and technical workshops to simulate a tech internship environment.",
        "Organized project showcases for students to present their minimum viable product to recruiters.",
        "Mentored student project teams in programming skills and product development."
      ]
    },
    "Paul G. Allen School of CSE - UW Seattle": {
      jobTitle: "CSE Student Ambassador @",
      duration: "MAR 2023 - PRESENT",
      desc: [
        "Coordinated K-12 computing outreach in Seattle schools, with a focus on supporting underrepresented students.",
        "Developed supplemental curriculum for partner high schools and an introductory JavaScript workshop.",
        "Supported students with post-secondary pathway exploration and pursuing a CS education at a 4 year university."
      ]
    },

  };

  const handleChange = (event, newValue) => {
    setValue(newValue);
  };

  return (
    <div className={classes.root}>
      <Tabs
        orientation={!isHorizontal ? "vertical" : null}
        variant={isHorizontal ? "fullWidth" : "scrollable"}
        value={value}
        onChange={handleChange}
        className={classes.tabs}
      >
        {Object.keys(experienceItems).map((key, i) => (
          <Tab label={isHorizontal ? `0${i}.` : key} {...a11yProps(i)} />
        ))}
      </Tabs>
      {Object.keys(experienceItems).map((key, i) => (
        <TabPanel value={value} index={i}>
          <span className="joblist-job-title">
            {experienceItems[key]["jobTitle"] + " "}
          </span>
          <span className="joblist-job-company">{key}</span>
          <div className="joblist-duration">
            {experienceItems[key]["duration"]}
          </div>
          <ul className="job-description">
            {experienceItems[key]["desc"].map(function (descItem, i) {
              return (
                <FadeInSection delay={`${i + 1}00ms`}>
                  <li key={i}>{descItem}</li>
                </FadeInSection>
              );
            })}
          </ul>
        </TabPanel>
      ))}
    </div>
  );
};

export default JobList;
